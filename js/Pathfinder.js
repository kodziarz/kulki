export default class Pathfinder {
    constructor(w, h) {
        this.paths = [];
        this.shortestPathsTo = [];
        this.findPath = (start, finish) => {
            this.start = start;
            this.finish = finish;
            if (start.getX() > 0) {
            }
        };
    }
}
